type StakingReward @entity {
  id: ID! #blockheight-eventIdx
  account: String!
  balance: BigInt!
  date: Date!
  blockHeight: Int!
}

type SumReward @entity{
  id: ID! # AccountId
  accountReward: BigInt!
  blockheight: Int!
}